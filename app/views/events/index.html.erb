<h1><%= I18n.t('title.event.events') %></h1>

<div class="buffer-both">
  <%= link_to I18n.t('buttons.create_event'), new_event_path,
    class: 'btn btn-success' %>
</div>

<div class="row buffer-bottom">
  <%= render partial: 'events/event', collection: @events %>
</div>
